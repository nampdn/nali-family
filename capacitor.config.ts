import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Nam & Linh Family',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
